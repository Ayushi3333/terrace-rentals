<div class="container mx-auto align-self-center w-50">
  <%= simple_form_for(@user) do |u| %>
    <%= u.input :full_name%>
    <%= u.input :description %>
    <%= u.input :photo, as: :file %>
    <%= u.submit "Update your details", class: "btn btn-flat center-button mb-3" %>
  <% end %>
</div>